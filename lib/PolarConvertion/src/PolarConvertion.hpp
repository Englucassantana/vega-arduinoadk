#include "../Vector2/scr/Vector2.hpp"
class PolarConvertion{
    public:
        static double getDistance(Vector2 target, Vector2 reference);
        static double getDegreeAngle(Vector2 target, Vector2 reference);
};